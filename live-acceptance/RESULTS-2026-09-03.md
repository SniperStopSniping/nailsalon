# Real-Clerk acceptance — 2026-09-03

Result: the complete real-account journey was verified in a persistent headed
browser, resuming the same account after harness-only assertion corrections.
This is **not** a claim that the original Playwright worker finished green, or
that production deployment and all release gates are complete.

## Scope

The release worktree product checkpoint was `365dd8051000b29a732971adeaa2cfffee3acbaa`:
Next.js 15.5.25, React 19.2.8, Clerk Next.js 6.39.6, Clerk Testing 1.14.9.
The app ran at `http://localhost:4211` with Clerk Development keys, a new
loopback PostgreSQL 16 database, and local media. The database had the verified
Development marker and all 75 migrations through `0074`, with no seed data.
No production database, provider media, real payment, email, or SMS credentials
were used. Port 4201 was untouched.

Run: `acceptance-05102775-7747-4708-b435-c26f88d43366`.
Evidence: `/tmp/luster-live-acceptance-TqDMxw/evidence/acceptance-05102775-7747-4708-b435-c26f88d43366/interactive/`.

## Verified on the same account and site

1. Quick Book basics, portrait, private-after-booking street address, online-only
   contact, opening hours, and style.
2. Real Development signup, email-code verification, organization selection,
   early authenticated draft claim, media upload, and media verification.
3. Services, booking preferences, about, layout, policies, and final claim:
   the site ID stayed the same and its revision increased.
4. After the saved celebration, authenticated status confirmed ready media,
   with no failed or pending media. A later read-only database check confirmed
   current revision 2, one ready media record, six active services, and free plan.
5. Free-plan PATCH returned 200; the real dashboard and dashboard reload worked.
   Authenticated handoff returned the same site, servicesAdded=true, and plan=free.
6. Saved preview displayed a loaded portrait from the same-origin server media
   route, not a blob URL or browser-only local storage.
7. Real logout, then password login through the visible Clerk form in a fresh
   context with no copied storage; the dashboard and saved server portrait returned.
8. Explicit publication changed only the disposable local fixture. Layout and
   salon publish endpoints returned 200. An unauthenticated guest reached the
   public page and booking page, selected a service, and advanced to appointment
   time selection. Availability returned 200. No booking was submitted.
9. The private street address was absent from the guest HTML.

## Diagnoses and harness corrections

- The official testing helper normalizes top-level client responses, but an
  unknown-email lookup also carries `meta.client`. Its false CAPTCHA-testing
  state interrupted this custom signup. Reloading the public Clerk client
  through the unmodified official helper restores its testing state. This is
  harness-only; no CAPTCHA or application protection was changed.
- The intended pending-session organization bootstrap now explicitly requests
  `auth({ treatPendingAsSignedOut: false })`. Verified identity, email, tenant,
  and wrong-user protections on the claim path remain enforced.
- File-backed PGlite aborted during Next development compilation. The isolated
  PostgreSQL runtime completed the real journey without that failure.
- The final button is Finish setup for an early-saved site. Core claim responses
  precede asynchronous media completion; the harness now waits for the saved UI
  and reads persisted status before asserting ready media.
- Chrome can release the plan response body during hard navigation. Its 200
  status and the subsequent persisted same-site free-plan record are checked.
- Canonical uploaded profile metadata uses the accessible image name
  Business owner portrait (media-port.ts); the saved renderer preserves that
  name. The corrected assertion requires visible, decoded, same-origin server
  media. No product accessibility text was changed.
- The Clerk instance renders email and password together on sign-in. The final
  return test used the actual password form, not a ticket or session shortcut.

## Privacy-mode distinction

An extra raw development-HTML check found owner fields in React 19 diagnostic
props for the **server** PublicSalonPageShell component. The shell already
projects explicit public values into its client provider. Independent execution
of the installed Flight serializer showed private sentinel fields in Development
and none in Production, while public content remained in both. This is not proof
of a production customer-data leak; no speculative projection rewrite was made.

A guarded read-only inspection of the hosted Preview database confirmed its
Preview marker but found no already-published salon to exercise. A production-mode
response check using the retained local fixture remains pending at this checkpoint.

## Validation and remaining limits

- Safety and sanitized-diagnostic tests: 7/7 passed; targeted harness ESLint and
  diff check passed.
- Organization-boundary and strict identity regression tests: 12/12 passed.
- AccountGate regression tests: 14/14 passed after SDK/React compatibility fixes.
- The complete journey above is headed-browser evidence with same-session
  continuation, not an uninterrupted all-green Playwright run.
- Real production signup, production media provider behavior, a completed guest
  booking, payment, and notification delivery are not established by this run.
- Generated passwords and browser credentials remained in memory. Auth traces
  are disabled; evidence contains screenshots and sanitized status/boolean data.
  Test users and organizations are retained pending explicit irreversible-cleanup
  consent; exact run-scoped cleanup targets are journaled privately. No reset,
  manual Backend API ticket, broad cleanup, or production test write was used.
