# Local real-Clerk acceptance — 2026-09-03

Result: **blocked before signup**. Do not treat this as a completed launch gate
or as proof that production signup is broken.

The tested app was the release worktree based on `c1f9bab`, with the in-progress
release changes, at `http://localhost:4211`. It used a new task-local PGlite
database and filesystem media directory outside the repository. Only Clerk
Development credentials were imported from the external Development dotenv
file. No production database, media, billing, email, or SMS credentials were
used. The server was stopped after the final attempt so release builds can use
the worktree's `.next` safely.

## Verified

- Guard tests: **3/3 passed**. Remote targets, live keys, real database/messaging
  credentials, arbitrary payment keys, and unsafe storage paths are rejected.
- AccountGate unit regression: **14/14 passed** after the runtime SDK update.
- Targeted acceptance-harness and AccountGate-test ESLint passed.
- Browser entry check: expected Quick Book controls rendered, meaningful
  content present, no Next error overlay.
- Official Clerk testing-token project setup passed.
- A separate no-navigation worker sanity check passed **2/2** with the updated
  helper: both `CLERK_TESTING_TOKEN` and `CLERK_FAPI` were present in the browser
  worker, and the official route helper registered successfully. Missing setup
  environment propagation is ruled out. Only presence booleans were recorded.
- Read-only comparison of the backend and frontend JWKS confirmed that the
  Development secret/public keys belong to the same instance. No key values
  were printed or recorded.

## Final real-browser result

Versions: `@clerk/nextjs 6.39.6`, `@clerk/testing 1.14.9`. The latter was refreshed
within the existing manifest range and its installed official helper includes
Clerk's CAPTCHA testing response handling. No application CAPTCHA configuration
was changed and no manual CAPTCHA bypass was attempted.

The final frozen-product run was
`acceptance-a4f779b2-b77d-4d43-82a5-4d227677388c`:

1. Quick Book basics, local portrait selection, address/contact, hours, style,
   and the email/password account gate were reached.
2. Clerk development-browser, environment, and client requests returned 200.
   The unknown-email sign-in lookup returned 422, as expected for this route to
   account creation.
3. After submitting the generated password, the UI remained at “Creating your
   account…” with a Cloudflare verification widget. Clerk's final
   `client.captchaBypass` was false and `signUp.status` was null.
4. No `/v1/client/sign_ups` response or application claim request was observed.
   There were no captured application API 5xx responses or page errors.
5. Exact run-scoped cleanup confirmed **0 users and 0 organizations removed**;
   the test did not create a completed identity.

The old helper's missing CAPTCHA response handling was a real harness issue,
but refreshing it alone did not clear this remaining browser-testing blocker.
The cause is not conclusively classified beyond the Clerk client/test-provider
boundary. Do not weaken production bot protection to make this test pass.

Sanitized evidence and the failure screenshot are under:

`/var/folders/r8/_8btv_bd56zb3m31sp0sycmw0000gn/T/luster-live-acceptance-flQIby/evidence/acceptance-a4f779b2-b77d-4d43-82a5-4d227677388c/pw-output/`

Auth traces are disabled; passwords and provider/session tokens were not
recorded. The helper's internal fetch means the browser response URL does not
show its injected testing token; a false `testingToken` field in the final
diagnostic is not evidence that the helper omitted it.

## Unverified gates

Real signup/email verification, early/final draft claims, saved server media,
services/about/layout/policies persistence, free-plan dashboard handoff,
reload/logout/fresh login, publication, public booking start, and a completed
guest booking remain unverified by this harness. The downstream test steps
exist but were not reached. These require a successful non-production auth
session before a launch-ready claim is justified.

Safest next step: reproduce the official helper's client state around the
unknown-email lookup, or have the user complete the provider's challenge in a
disposable Development account. An existing Development owner can verify a
login path, but must not be used for tenant/org mutations without a specifically
isolated fixture. Never substitute a production account or production writes.
