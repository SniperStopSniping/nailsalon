// Compatibility alias for saved clients-workspace URLs and older consumers.
// Client Insights is the canonical implementation and owns all calculations.
export const dynamic = 'force-dynamic';

export { GET } from '@/app/api/admin/client-insights/route';
