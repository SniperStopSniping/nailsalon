# Canonical local setup template.
#
# Copy this file to .env.development.local. The guarded development database
# wrapper loads that file first, then fills missing values from .env.local,
# without overriding values already exported by the invoking process.
# Every value below is fake or local-only.
# Local Development must use local or disposable infrastructure; never point it
# at a Production database, cache, or provider account.

# Required for local builds
BILLING_PLAN_ENV=dev
# Use Clerk Development-instance keys only.
CLERK_SECRET_KEY=sk_test_replace-with-clerk-development-secret
# This synthetic publishable key resolves only to clerk-dev.invalid.
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=pk_test_Y2xlcmstZGV2LmludmFsaWQk
NEXT_PUBLIC_CLERK_SIGN_IN_URL=/sign-in
# Use Stripe test-mode keys and a test-mode webhook endpoint only.
STRIPE_SECRET_KEY=sk_test_replace-with-stripe-test-secret
STRIPE_WEBHOOK_SECRET=whsec_replace-with-stripe-test-webhook-secret
# Signing secret for the SEPARATE Connect-scoped webhook endpoint. Must differ
# from STRIPE_WEBHOOK_SECRET above; sharing one is rejected at boot. Locally:
#   stripe listen --forward-connect-to localhost:3000/api/webhooks/stripe-connect
STRIPE_CONNECT_WEBHOOK_SECRET=whsec_replace-with-stripe-test-connect-webhook-secret
# ABSENT MEANS ENABLED. Set to "false" to stop DISPATCHING Connect webhook
# events while still verifying and persisting them. Never blank the secret above
# to disable — that 503s every delivery and destroys the event evidence trail.
DEPOSITS_CONNECT_WEBHOOK_PROCESSING_ENABLED=
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_replace-with-stripe-test-publishable-key

# Optional local-only application settings and disposable infrastructure
NEXT_PUBLIC_APP_URL=http://localhost:3000
# Leave blank for the safest default: isolated in-memory PGlite. To use a
# persistent local PostgreSQL database, set a loopback URL and run the guarded
# initialize/migrate/seed sequence before starting the app.
DATABASE_URL=
# Leave blank for loopback. For an approved hosted Development database, enter
# only its exact hostname (no scheme, wildcard, port, path, or credentials).
LUSTER_NONPROD_DB_HOSTS=
DATABASE_POOL_MAX=2
REDIS_URL=redis://127.0.0.1:6379
APP_ENV=development
LUSTER_ROOT_DOMAIN=example.invalid
TENANT_SUBDOMAINS_ENABLED=false
TENANT_SUBDOMAIN_ALLOWLIST=
CLERK_AUTHORIZED_PARTIES=http://localhost:3000
LEGACY_OTP_AUTH_ENABLED=false
SUPER_ADMIN_BOOTSTRAP_PHONE=
DEV_SUPER_ADMIN_ID=dev-super-admin

# Hosted-only settings (configure in the hosting provider, not in a tracked file)
CRON_SECRET=replace-with-hosted-random-secret
# Generated by the two Checkly heartbeat-as-code resources. Install only the
# matching generated URL in each Production environment variable.
CHECKLY_DEPOSIT_RECONCILE_HEARTBEAT_URL=
CHECKLY_INTEGRATION_OUTBOX_HEARTBEAT_URL=
INTEGRATION_ENCRYPTION_KEY=replace-with-hosted-random-encryption-key
INTEGRATION_ENCRYPTION_KEY_VERSION=1
OAUTH_STATE_SECRET=replace-with-hosted-random-oauth-state-secret
NEXT_PUBLIC_SENTRY_DSN=https://public@example.invalid/1
SENTRY_ORG=example-organization
SENTRY_PROJECT=example-project
SENTRY_AUTH_TOKEN=replace-with-sentry-auth-token
SENTRY_RELEASE=
SENTRY_ENVIRONMENT=
RESEND_API_KEY=replace-with-resend-api-key
RESEND_FROM_EMAIL=notifications@example.invalid
RESEND_REPLY_TO_EMAIL=replies@example.invalid

# Optional salon integrations
TWILIO_ACCOUNT_SID=replace-with-twilio-account-id
TWILIO_AUTH_TOKEN=replace-with-twilio-auth-token
TWILIO_VERIFY_SERVICE_SID=replace-with-twilio-verify-service-id
TWILIO_PHONE_NUMBER=+15550100100
TWILIO_CONNECT_APP_SID=replace-with-twilio-connect-app-id
TWILIO_CONNECT_REDIRECT_URI=https://app.example.invalid/api/integrations/twilio/callback
GOOGLE_OAUTH_CLIENT_ID=replace-with-google-oauth-client-id
GOOGLE_OAUTH_CLIENT_SECRET=replace-with-google-oauth-client-secret
GOOGLE_OAUTH_REDIRECT_URI=https://app.example.invalid/api/integrations/google/callback
META_APP_ID=000000000000000
META_APP_SECRET=replace-with-meta-app-secret
META_SYSTEM_USER_TOKEN=replace-with-meta-system-user-token
META_FACEBOOK_PAGE_ID=000000000000000
META_INSTAGRAM_ACCOUNT_ID=000000000000000
META_GRAPH_VERSION=v19.0
CLOUDINARY_CLOUD_NAME=example-cloud
CLOUDINARY_API_KEY=replace-with-cloudinary-public-id
CLOUDINARY_API_SECRET=replace-with-cloudinary-api-secret

# Customer identity, customer OTP, and customer-session variables are
# intentionally absent because those capabilities are not available yet.
#
# Removing values from the current tree does not remove historical exposure.
# Any still-active historical credential needs provider-side rotation or
# revocation. Rewriting history requires separate owner approval and does not
# replace rotation.

# --- Shared Luster-managed SMS (Gate A foundation; ALL optional, dark by default) ---
# Shared Messaging Service for the Luster-owned sender. Setting this alone
# does NOT enable sending (see COMMUNICATIONS_SMS_ENABLED and the platform
# communication control, which ships disabled).
TWILIO_MESSAGING_SERVICE_SID=
# Logical sender identity that keys global STOP suppression. Changing it
# orphans every existing opt-out — one-way operational door. Default:
# luster_shared_v1
LUSTER_SMS_SENDER_IDENTITY=
# Platform hard stop for the shared sender. Unset or anything other than
# 'true' means the shared path is structurally dark.
# COMMUNICATIONS_SMS_ENABLED=true
# Gates NEW Twilio Connect onboarding (connect/provision routes) only.
# Existing connected BYO salons keep working regardless of this flag.
# SMS_BYO_MODE_ENABLED=true
# Controlled pilot mode: when 'true', only salon slugs in the allowlist may
# use the shared sender. Enabled with an empty allowlist means NOBODY.
# SMS_PILOT_ENABLED=true
SMS_PILOT_SALON_ALLOWLIST=

# --- Billing business-identity fingerprints (Gate B; optional, fail-closed) ---
# Dedicated HMAC secret for verified-email identity links. Never reuse OAuth/
# Clerk/Stripe/integration secrets. Rotation: bump the version — new links
# attach to the SAME business identity; never reset eligibility.
BILLING_IDENTITY_HMAC_SECRET=
BILLING_IDENTITY_HMAC_VERSION=

# Fixed Luster-owned origin for short manage links (/a/<token>). Host must be
# <= 23 chars for the one-segment SMS budget (template tests enforce this).
# Never a salon custom domain.
LUSTER_SHORT_LINK_ORIGIN=
